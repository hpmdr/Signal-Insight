# 2026-06-08 工作日志

## Android 开发环境搭建

### 完成的工作
- 配置 Android SDK 环境（路径：C:\Users\hpmdr\app\Android）
- 接受所有 Android SDK 许可协议
- 安装必要的 SDK 组件：
  - platform-tools
  - build-tools;36.0.0
  - platforms;android-36
  - platforms;android-31
- 配置环境变量：
  - ANDROID_HOME = C:\Users\hpmdr\app\Android
  - PATH 添加：C:\Users\hpmdr\app\Android\cmdline-tools\latest\bin 和 platform-tools
- 创建 local.properties 文件（sdk.dir=C:/Users/hpmdr/app/Android）
- 成功构建项目（./gradlew build）

### 项目信息
- **项目名称**：Signal Insight
- **包名**：cn.debubu.signalinsight
- **技术栈**：Kotlin + Jetpack Compose
- **JDK**：Microsoft JDK 21.0.11（路径：C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot）
- **Gradle 版本**：8.13
- **编译 SDK**：36
- **最小 SDK**：31
- **目标 SDK**：36

## 国际化改造（2026-06-08 下午）

### 完成的工作
- 创建 `values/strings.xml`（中文，140 行，80+ 字符串）
- 创建 `values-en/strings.xml`（英文，140 行，完整翻译）
- 改造所有 Kotlin 文件，使用 `stringResource(R.string.xxx)` 引用

### 修改的文件
1. **MainScreen.kt** - 10 处硬编码文本替换
2. **PermissionScreen.kt** - 6 处替换 + PermissionItem 资源 ID 化
3. **PermissionViewModel.kt** - PermissionRequirement 字段改为资源 ID
4. **CellularPage.kt** - 最大改动：
   - MetricInfo 数据类字段改为 Int 资源 ID
   - RangeStep.label 改为 labelResId: Int
   - 8 个指标的科普数据库全部使用 R.string.xxx
   - 12 处 UI 文本替换
   - Explainer 内容渲染改为 stringResource 解析
5. **CellularViewModel.kt** - 改为 AndroidViewModel，使用 context.getString()
6. **CellularSignalModel.kt** - 运营商名改为英文（作为数据层内部表示）
7. **MainActivity.kt** - ViewModelFactory 适配 AndroidViewModel

### 构建验证
- ✅ BUILD SUCCESSFUL（10 秒）
- ✅ 无错误、无警告
- ✅ 已安装到手机（2311DRK48C）

### BandExplainer Demo（2026-06-08 下午）

### 完成的工作
- 新增 `BandExplainer.kt` - 独立的频段详细科普组件
- 新增 40+ 条 Band 相关字符串资源（中英文）
- 修改底部弹窗分发逻辑，支持按指标名分发到不同组件

### BandExplainer 包含的内容
1. **基本概念** - 频段是什么
2. **当前连接频段** - 显示当前频段并分类（低频/中频/高频）
3. **频段特性对比表** - 低频/中频/高频的覆盖、穿墙、速度三维对比
4. **运营商频段分布表** - 移动/联通/电信/广电的 5G/4G 频段清单
5. **频段评价** - 根据当前频段给出体验建议
6. Tip 提示

### 未改动
- "dBm"/"dB" 技术单位（无需翻译）
- CompactSimSwitcher 默认参数（实际调用覆盖）

### 修复的弃用 API
1. **CellularRepository.kt:114**
   - 问题：`tm.networkType` 已弃用（Android R 以上）
   - 修复：使用 `tm.dataNetworkType`（API 30+），低版本继续使用 `networkType` 并添加 `@Suppress("DEPRECATION")`
   - 影响：消除构建警告，代码兼容 Android 11+

2. **CellularPage.kt:555**
   - 问题：`Divider()` 已重命名为 `HorizontalDivider()`
   - 修复：改为 `HorizontalDivider()`
   - 影响：消除构建警告，符合 Material3 最新 API

### 构建验证
- ✅ 构建成功（BUILD SUCCESSFUL in 1s）
- ✅ 无弃用警告
- ✅ 所有 36 个任务 up-to-date
- ✅ 已安装到手机（2311DRK48C）验证

## 已知问题（待修复）

### 测试任务失败
- **原因**：文件被其他进程锁定
- **解决方案**：
  ```bash
  ./gradlew --stop
  ./gradlew clean
  ./gradlew build
  ```

## 7 个参数科普弹窗（完整版）

### 新建的文件
```
app/src/main/java/cn/debubu/signalinsight/ui/cellular/
├── RSRPExplainer.kt      # RSRP: 5级信号范围 + 影响因素 + 评估
├── RSRQExplainer.kt      # RSRQ: 3级质量 + RSRP联合判断 + 评估
├── SINRExplainer.kt      # SINR: 5级速度对应表 + 影响因素 + 评估
├── RSSIExplainer.kt      # RSSI: RSRP对比 + 适用场景 + 评估
├── PCIExplainer.kt       # PCI: 切换原理 + Mod3干扰 + 评估
├── EARFCNExplainer.kt    # EARFCN: 频率公式 + 运营商频点 + 评估
├── TACExplainer.kt       # TAC: 更新场景 + 作用说明 + 评估
└── ExplainerUtils.kt     # 共享 SectionCard 组件
```

### 架构优化
- 每个参数一个独立文件
- 共享 SectionCard 组件（ExplainerUtils.kt）
- 底部弹窗使用 when(key) 分发到对应组件
- 所有文本中英文双语（strings.xml + values-en/strings.xml）

## 深色模式颜色修复

### 修复内容（51处替换）
- Tip 卡片：`Color(0xFFE3F2FD)` → `MaterialTheme.colorScheme.tertiaryContainer`
- 评估卡片（绿色）：`Color(0xFFE8F5E9)` → `MaterialTheme.colorScheme.primaryContainer`
- 评估卡片（橙色）：`Color(0xFFFFF3E0)` → `MaterialTheme.colorScheme.tertiaryContainer`
- 评估卡片（红色）：`Color(0xFFFFEBEE)` → `MaterialTheme.colorScheme.errorContainer`
- 对比表文字：硬编码绿/红色 → `MaterialTheme.colorScheme.primary` / `error`
- 受影响的文件：全部 8 个 Explainer

## LTE RSSNR 数据源修复

### 修复内容
- LTE 的 sinr 字段从默认值 -20 改为读取 `signalStrengthLte.rssnr`
- 增加 Int.MAX_VALUE 无效值检测
- 服务小区 + 邻小区两处同时修复

### API 验证
- `CellSignalStrengthLte.getRssnr()` - API 17, minSdk 31 ✅
- 4G 下显示 RSSNR 作为 SINR 的等效指标
- Band 频段已使用 `bands.firstOrNull()` 从 API 获取真实值，无需修改
