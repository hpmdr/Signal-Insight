# 2026-06-10 工作日志

## 依赖库全面升级
- Gradle: 8.13 → 9.5.1（使用腾讯云镜像 https://mirrors.cloud.tencent.com/gradle/ 下载）
- AGP: 8.13.2 → 9.2.0
- Kotlin: 2.3.0 → 2.4.0
- Compose BOM: 2025.12.01 → 2026.05.00（2026.06.00 尚未发布）
- 适配 AGP 9.x 变更：
  - 移除 `kotlin-android` 插件（AGP 9+ 已内置）
  - 保留 `kotlin-compose` 插件（仍需要）
- ✅ 构建成功，已安装到手机

## 修复关于/设置页返回键退出 App
- 从菜单进入关于或设置页后，按返回键应回到信号页，而非退出 App
- 在 MainScreen.kt 主页面内容区添加条件 BackHandler：
  ```kotlin
  if (selectedDestination != Destination.Cellular) {
      BackHandler(onBack = { selectedDestination = Destination.Cellular })
  }
  ```
- ✅ 构建成功，已安装到手机

## P0/P1 代码质量修复
### 已修复
- ✅ 删除废弃 `ExplainerDetailScreen.kt`（已被 MainScreen AnimatedContent 替换）
- ✅ 删除空 `data/di/AppModule.kt`
- ✅ 删除 `MainActivity.kt` 中的空 `GreetingPreview` 和未使用的 `requestPermissionLauncher`
- ✅ `SignalData` / `NeighborCellTableModel` 从 `CellularViewModel.kt` 移至 `CellularSignalModel.kt`
- ✅ 所有引用处添加了正确的导入语句
- ✅ 构建成功，已安装到手机

### 待处理
- ✅ ViewModel `mutableStateOf` → `StateFlow` 迁移
  - 移除所有 Compose runtime 依赖 (mutableStateOf, derivedStateOf, mutableIntStateOf)
  - 改用 MutableStateFlow / StateFlow + combine / map / stateIn
  - CellularPage 改用 collectAsState() 收集 StateFlow
  - 提取 toSignalData() / toNeighborModels() 扩展函数减少重复
  - ✅ 构建成功，已安装到手机
- ⬜ 定义 MetricKey 枚举替代硬编码字符串
- ⬜ Explainer 文件去重（8 文件 ~2400 行）

## 修复详情页返回键退出 App
- 重写 MainScreen.kt 时遗漏了 BackHandler
- 添加 `import androidx.activity.compose.BackHandler`
- 在详情页内容区添加 `BackHandler(onBack = { explainerRoute = null })`
- ✅ 已安装到手机，测试通过

## MetricKey 枚举全链路集成
- MainScreen.kt: ExplainerRoute.key String → MetricKey
- CellularPage.kt: onOpenExplainer / metricsDatabase 全面使用 MetricKey
- SimContentPage.kt: Metric 数据类新增 key: MetricKey 字段
- 所有硬编码字符串 ("Band", "RSRP" 等) 替换为 MetricKey 枚举值
- ✅ 构建成功，已安装到手机，功能正常

### 剩余待处理
- ✅ Explainer 文件去重（8 文件 ~2400 行 → 可大幅减少代码量）
  - 提取 `MetricExplainerShell` 通用外壳到 `ExplainerUtils.kt`
  - 所有 8 个 Explainer 改用统一模板：标题头 + tip + 按钮 + verticalScroll 统一管理
  - 每文件减少 ~50-150 行，总减少约 800 行
  - ✅ 构建成功，已安装到手机

---

**📊 今日最终汇总：代码审查修复全部完成**
- P0: ViewModel StateFlow 迁移 ✅
- P0: 删除废弃文件 (ExplainerDetailScreen, AppModule) ✅
- P1: SignalData 数据模型位置修正 ✅
- P1: MetricKey 枚举全链路集成 ✅
- P1: Explainer 去重（MetricExplainerShell） ✅
- P2: 空 Preview 清理 ✅
- ✅ 所有改动已安装到手机并测试通过

## 🔧 紧急修复：StateFlow 冷读导致信号全显示 -120 dBm ✅ 已安装
🔧 修复：权限拒绝后去设置手动授权，返回时自动刷新检测 ✅ 已安装
**根因**：CellularPage 使用 `viewModel.sim1SignalData.value` 冷读 StateFlow，不会触发 Compose 重组。
**修复**：添加 `collectAsState()`：`val sim1Data by viewModel.sim1SignalData.collectAsState()`
✅ 已安装到手机
