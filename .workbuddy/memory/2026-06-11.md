# 2026-06-11 工作日志

## 自动化测试诊断（MIUI 兼容性问题）
- Compose UI Test (createComposeRule / createAndroidComposeRule) 在 MIUI 上全部挂起
- 根因：MIUI Enhanced Confirmation 机制拦截测试框架 Activity
- 定论：MIUI 设备无法运行 Compose UI Test，需用模拟器或非 MIUI 设备
- PermissionFlowTest 代码保留，换环境直接 `./gradlew connectedDebugAndroidTest` 即可

## 底部导航运营商名修复
- 根因：derivedStateOf 冷读 StateFlow.value（viewModel.getSimOperatorName()）
- 修复：改用已 collectAsState() 的 sim1Data.operatorName
- 底层原理：StateFlow.value 不被 Compose Snapshot 追踪，collectAsState() 才是桥梁

## 数据流参数审查（全链路、逐参数）
- 确认所有 10 个参数（RSRP/RSRQ/SINR/RSSI/PCI/EARFCN/Band/TAC/运营商/网络类型）链路畅通
- 无其他 StateFlow 冷读 bug

## 生命周期适配（后台暂停、前台恢复）
- 新增 CellularViewModel.pauseDataCollection() / resumeDataCollection()
- MainScreen LifecycleEventObserver 扩展为 ON_PAUSE 暂停 / ON_RESUME 恢复
- 后台不再浪费电量和系统资源

## RSSI 修复（5G NR 无 RSSI 指标）
- 根因：CellSignalStrengthNr 没有 rssi 属性，NR 分支未设 rssi → 永远默认 -120
- 修复：NR/WCDMA/LTE 均增加 Int.MAX_VALUE 检测
- LTE rssi 增加无效值回退

## 5G 指标标签区分（SS-RSRP/SS-RSRQ/SS-SINR）
- 4G LTE: 显示 RSRP / RSRQ / SINR
- 5G NR: 显示 SS-RSRP / SS-RSRQ / SS-SINR
- SimContentPage 和 NeighborCellsCard 表头均已适配

## 按网络类型分别处理参数（基于 Android 官方 API 文档）
- 查阅 developer.android.com 官方文档确认各 API 参数范围
- SignalData / CellularSignalInfo 默认值从 -120/-20 改为 Int.MAX_VALUE
- 各网络类型 fromCellInfo() 只填充可用字段
- UI 显示 N/A 当值为 Int.MAX_VALUE
- 信号环 dbm 为 MAX_VALUE 时显示 N/A + 灰色

## 项目记忆文档更新
- 完整数据流架构（4 个步骤：监听→提取→ViewModel→UI）
- 各网络类型参数对比表
- StateFlow → Compose 常见陷阱
