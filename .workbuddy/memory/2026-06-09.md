# 2026-06-09 工作日志

## 底部导航改为 Material3 Tab 样式（方案 D）
- 原药丸式分段按钮（方案 C）改为文字 + 底部滑动指示条
- 使用 `animateDpAsState` + `spring` 弹簧动画，指示条平滑滑动
- 文字颜色使用 `animateColorAsState` 渐变过渡
- 修复指示条偏移基准点：`BottomCenter` → `BottomStart`
- 后续优化：指示条宽度改为屏幕 1/4，上移 8dp 贴近文字
- build ✅ 已安装到主力机

## 科普弹窗改为全屏详情页（方案 A）
### 问题
- 8 个科普弹窗每个 200-385 行，含 4-5 个 SectionCard + 表格 + 长文本
- ModalBottomSheet 不适合承载大量教育性内容
- Android Material Design 3 指南建议长内容使用全屏页面

### 修改
- **新建 `ExplainerDetailScreen.kt`**：
  - 全屏页面，Scaffold + TopAppBar（返回箭头 + "RSRP 详解"格式标题）
  - verticalScroll 可滚动内容，底部间距 32dp
  - 根据 key 分发到对应 Explainer 组件，onClose 映射为 onBack

- **`CellularPage.kt`**：
  - 移除 `selectedMetricKey`、`showSheet`、`sheetState` 状态
  - 移除整个 ModalBottomSheet 代码块
  - 新增 `onOpenExplainer: (key, signalData)` 回调参数
  - 清理不再使用的 import

- **`MainScreen.kt`**：
  - 新增 `selectedExplainerKey` / `explainerSignalData` 状态
  - 当详情页激活时，完全替换外层 Scaffold（避免嵌套）
  - CellularPage 调用处传入 onOpenExplainer

- **strings.xml / values-en/strings.xml**：
  - 新增 `back`（返回）、`explainer_detail_title_suffix`（详解/Details）

### 架构变化
```
之前: 指标点击 → ModalBottomSheet（弹窗覆盖）
之后: 指标点击 → MainScreen 切换 → ExplainerDetailScreen（全屏页面替换）
  └── 返回时恢复 MainScreen 正常内容
```

### 构建验证
✅ BUILD SUCCESSFUL in 21s
✅ 已安装到主力机

### 崩潰修复（Explainer 详情页嵌套 verticalScroll）
- **原因**：ExplainerDetailScreen 的 Column 有 `verticalScroll`，内部的 BandExplainer 等也有 `verticalScroll`，两层嵌套导致 `IllegalStateException`
- **修复**：移除 ExplainerDetailScreen 的 `verticalScroll`

### 体验优化
- **内容贴顶**：ExplainerDetailScreen 内容顶部添加 `Spacer(16.dp)`，不再紧贴标题栏
- **侧滑/返回退出 App**：
  - `BackHandler(onBack)` 拦截系统返回键 → 回到主页面而非退出 App
  - `ModalNavigationDrawer(gesturesEnabled = !isShowingExplainer())` 详情页显示时禁用抽屉手势

## AnimatedContent 转场动画添加及编译错误修复
- 用户请求添加主页面与详情页之间的转场动画
- 添加 AnimatedContent 包裹，使用 slideInHorizontally + fadeIn + slideOutHorizontally + fadeOut
- 修复多个编译错误：
  - AnimatedContent transitionSpec 语法错误（去掉多余括号）
  - 类型推断失败（给 selectedDestination 添加类型注解 `Destination`）
  - NavigationItem/Destination 未解析（将定义移到文件开头 import 之后）
  - AboutScreen/SettingsScreen 未解析（将函数定义移到 MainScreen 之前）
  - 文件末尾缺少大括号（添加缺失的 `}`）
- ✅ 构建成功
- AnimatedContent 转场动画已生效：主页面 ↔ 详情页切换时有左右滑动 + 淡入淡出效果

